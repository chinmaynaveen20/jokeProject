<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dark Humor Joke Generator</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;700&display=swap" rel="stylesheet">
    <style>
        body {
            background-color: #121212;
            color: #ffffff;
            font-family: 'Roboto', sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            overflow: hidden;
        }

        h1 {
            font-size: 3em;
            margin-bottom: 20px;
            text-transform: uppercase;
            letter-spacing: 2px;
            border-bottom: 2px solid #333;
            padding-bottom: 10px;
        }

        button {
            background-color: #1f1f1f;
            color: #ffffff;
            border: none;
            padding: 15px 30px;
            font-size: 1.2em;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.3s ease;
            border-radius: 5px;
            margin-bottom: 20px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        button:hover {
            background-color: #333333;
            transform: scale(1.05);
        }

        h2, h4, h3 {
            margin: 20px 0;
            text-align: center;
        }

        h2 {
            font-size: 2em;
            color: #f39c12;
        }

        h4, h3 {
            font-size: 1.5em;
            color: #ecf0f1;
        }

        .container {
            text-align: center;
            max-width: 80%;
            margin: auto;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Dark Humor Joke Generator</h1>
        <button id="generateJoke" onclick="window.location.href='/submit'">Generate Joke</button>
        <% if (locals.type == "twopart"){ %>
            <h2><%= locals.setup %></h2>
            <h4><%= locals.delivery %></h4>
        <% } else if (locals.type == "single"){ %>
            <h3><%= locals.joke %></h3>
        <% } %>
    </div>
</body>
</html>
